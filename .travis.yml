language: php
php:
  - 5.4
  - 5.5

before_script:
  - wget http://getcomposer.org/composer.phar
  - php composer.phar install --dev

script:
  - ./vendor/bin/phpcs --ignore="*/Tests/*" --standard=vendor/internations/kodierungsregelwerksammlung/ruleset.xml ./src/
  - phpunit

notifications:
  email:
    - max.beutel@internations.org
    - lars.strojny@internations.org
